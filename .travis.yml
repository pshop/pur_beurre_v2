language: python
python:
    - '3.6.6'

before_script:
    - pip install -r requirement.txt

env: DJANGO_SETTINGS_MODULE="pur_beurre_v2.settings.travis"

services:
    - postgresql

script:
    - ./manage.py test

branches:
    only:
        - staging

